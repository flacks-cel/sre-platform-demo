{{- /*
ServiceAccount is created by Terraform (main.tf).
This file is intentionally empty to document that decision.

To inspect the existing ServiceAccount:
  kubectl get serviceaccount jobs-api -n app
*/ -}}
